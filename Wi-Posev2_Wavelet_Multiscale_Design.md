# 基于小波多尺度特征的 WiFi CSI 二维人体姿态估计系统设计文档

## 1. 文档目的

本文档给出一个完整、独立、可直接用于实现的模型设计，用于从 WiFi CSI amplitude 信号预测 Human3.6M-17 二维人体关键点。

文档不依赖读者预先了解任何旧项目、旧模型名称或内部缩写。所有模块都从输入、张量形状、具体操作、设计理由和预期作用开始重新定义。

本文档对应的核心研究问题是：

> 在输入只有三根接收天线的 CSI amplitude，并且每个姿态样本只有 10 个原始 CSI packet 的情况下，如何通过傅里叶重采样、小波多尺度特征、粗细双分支编码和关节条件解码，提高人体躯干、四肢中段以及手腕、脚踝等远端关节的二维姿态估计性能。

---

# 2. 任务定义与固定前提

## 2.1 输入数据

每一个姿态样本对应 10 个原始 CSI packet。

单个原始 CSI packet 包含：

```text
3 根接收天线 × 114 个子载波
```

因此，原始单样本可以表示为：

```text
X_original ∈ R^[3, 114, 10]
```

其中：

| 维度 | 大小 | 含义 |
|---|---:|---|
| 天线维 | 3 | 三根接收天线 |
| 子载波维 | 114 | 每根天线上的 CSI 子载波 |
| 时间维 | 10 | 与一个姿态样本对应的 10 个原始 packet |

项目只使用 CSI amplitude，不使用 CSI phase。

---

## 2.2 傅里叶重采样

为了统一时间长度并为后续卷积、注意力和小波分解提供更密集的时序表示，沿时间轴将 10 个原始采样点傅里叶重采样到 64 个采样点：

```text
[3, 114, 10]
    ↓ Fourier resampling along time
[3, 114, 64]
```

组成 batch 后：

```text
X ∈ R^[B, 3, 114, 64]
```

本文档统一采用以下张量布局：

```text
[B, C, F, T]
```

其中：

| 符号 | 含义 |
|---|---|
| `B` | batch size |
| `C` | 天线或特征通道 |
| `F` | 子载波位置 |
| `T` | 时间采样位置 |

因此：

```text
C = 3
F = 114
T = 64
```

需要明确：

> 64 个时间点是由 10 个真实 CSI packet 重采样得到的表示，不是 64 个独立采集的真实 packet。

## 2.3 数据构建阶段的训练输入协议

傅里叶重采样在数据构建阶段完成，而不是在训练时的 `Dataset` 或模型前向传播中完成。

数据构建脚本读取原始 MAT 文件中的：

```text
CSIamp ∈ R^[3,114,10]
```

沿时间轴执行傅里叶重采样：

```text
[3,114,10] → [3,114,64]
```

然后保存为训练直接读取的 memmap / npy 文件：

```text
X_amp_resampled.npy ∈ R^[N,3,114,64]
Y_2d_clean.npy      ∈ R^[N,17,2]
meta.npz            frame-aligned metadata
```

其中 `N` 是全部帧样本数量。`X_amp_resampled.npy` 的存储布局必须与模型输入一致：

```text
[sample, antenna, subcarrier, time]
```

训练阶段的 dataloader 直接加载 `[3,114,64]`，不再执行傅里叶重采样，也不再在 batch 内频繁转置时间轴和子载波轴。

这样做的目的：

1. 节约训练时间；
2. 固定模型输入接口；
3. 降低 `[time, antenna, subcarrier]` 与 `[antenna, subcarrier, time]` 混用风险；
4. 使小波分解始终沿最后一维 `time=64` 执行。

---

## 2.4 输出定义

模型输出 Human3.6M-17 二维人体姿态：

```text
P_pred ∈ R^[B, 17, 2]
```

最后一维为：

```text
(x, y)
```

17 个关节索引如下：

| 索引 | 关节 |
|---:|---|
| 0 | pelvis / root |
| 1 | right_hip |
| 2 | right_knee |
| 3 | right_ankle |
| 4 | left_hip |
| 5 | left_knee |
| 6 | left_ankle |
| 7 | spine |
| 8 | thorax / chest |
| 9 | neck |
| 10 | head |
| 11 | left_shoulder |
| 12 | left_elbow |
| 13 | left_wrist |
| 14 | right_shoulder |
| 15 | right_elbow |
| 16 | right_wrist |

骨架边定义为：

```python
H36M17_EDGES = (
    (0, 1), (1, 2), (2, 3),
    (0, 4), (4, 5), (5, 6),
    (0, 7), (7, 8), (8, 9), (9, 10),
    (8, 11), (11, 12), (12, 13),
    (8, 14), (14, 15), (15, 16),
)
```

---

# 3. 设计问题分析

## 3.1 原始卷积特征提取的局限

基础卷积网络通常采用：

```text
局部卷积
    ↓
下采样
    ↓
更深卷积
    ↓
单一最终特征图
```

这种结构虽然会通过不同深度形成不同感受野，但存在三个问题。

### 问题一：多尺度信息没有显式保留

浅层卷积包含细粒度局部信息，深层卷积包含更粗粒度结构信息。但如果模型只保留最终深层特征图，浅层细节会被逐步压缩。

### 问题二：时间轴下采样过快

如果对时间轴连续使用步长为 2 的卷积，时间长度会：

```text
64 → 32 → 16
```

对于躯干和整体姿态，16 个时间 token 可能足够；但手腕、脚踝等远端关节对应的 CSI 扰动通常更弱、更局部，也更容易在下采样中被平均。

### 问题三：所有关节读取同一尺度特征

如果 17 个关节全部从同一组特征 token 中解码，模型无法显式区分：

```text
骨盆、脊柱、胸腔需要的粗粒度结构信息
```

与：

```text
手腕、脚踝需要的细粒度局部信息
```

因此，本设计的核心不是简单增加一个小波预处理，而是建立一条完整的多尺度信息路径：

```text
小波多尺度分解
    ↓
粗尺度与细尺度分别编码
    ↓
每个关节独立选择尺度
    ↓
粗姿态预测与细尺度残差修正
```

---

# 4. 总体模型结构

完整模型包含六个阶段：

```text
阶段 1：输入归一化
阶段 2：时间轴小波多尺度特征提取
阶段 3：粗尺度和细尺度特征融合
阶段 4：粗细双分支卷积编码
阶段 5：粗细双分支轴向注意力编码
阶段 6：关节条件的粗细尺度解码
```

整体数据流如下：

```text
CSI amplitude
[B,3,114,64]
        │
        ├──────────────────── Raw
        │
        └── 时间轴三级平稳小波变换
              ├── A3
              ├── D3
              ├── D2
              └── D1

Raw + A3 + D3
        ↓
粗尺度融合
[B,32,114,64]
        ↓
粗尺度卷积编码
[B,128,29,16]
        ↓
粗尺度轴向注意力编码
[B,256,29,16]
        ↓
粗尺度 token
[B,464,256]

Raw + D2 + D1
        ↓
细尺度融合
[B,32,114,64]
        ↓
细尺度卷积编码
[B,128,29,32]
        ↓
细尺度轴向注意力编码
[B,256,29,32]
        ↓
细尺度 token
[B,928,256]

17 个 Human3.6M 关节查询
        ↓
先读取粗尺度 token，预测完整基础姿态
        ↓
再读取细尺度 token，预测关节残差
        ↓
每个关节学习独立的细尺度门控
        ↓
Human3.6M 骨架图传播
        ↓
最终输出
[B,17,2]
```

---

# 5. 阶段一：输入归一化

## 5.1 输入

```text
X ∈ R^[B,3,114,64]
```

## 5.2 推荐归一化方法

推荐使用只基于训练集统计量的全局 Z-score：

```text
X_norm = (X - μ_train) / (σ_train + ε)
```

其中：

```text
μ_train, σ_train
```

只由训练集计算，不能使用验证集或测试集统计量。

统计量可以有两种粒度。

### 方案 A：单一全局统计量

```text
μ_train ∈ R
σ_train ∈ R
```

优点是简单、稳定。

### 方案 B：按天线和子载波统计

```text
μ_train ∈ R^[1,3,114,1]
σ_train ∈ R^[1,3,114,1]
```

优点是可以补偿不同天线、不同子载波的固定幅值差异。

建议第一版采用方案 B，因为它保留时间变化，同时去除各天线和子载波的静态尺度偏置。

重要约束：

> 数据构建阶段只保存傅里叶重采样后的 amplitude，不保存已经按训练 split 归一化后的输入。

原因是本项目需要支持 leave-one-environment-out、source-only 和 few-shot finetune。不同训练 split 的训练集不同，因此 `μ_train` 和 `σ_train` 也不同。如果在全量数据构建阶段提前归一化，会引入验证集或测试环境统计量泄漏。

推荐实现方式：

1. `X_amp_resampled.npy` 保存未做最终训练归一化的 float32 amplitude；
2. 每个训练 run 根据当前 split 的训练 frame indices 计算 `μ_train` 和 `σ_train`；
3. 将统计量保存到该 run 的输出目录，例如 `normalization_stats.json` 或 `normalization_stats.npz`；
4. dataloader 或模型入口使用当前 run 的统计量执行 Z-score；
5. validation/test/few-shot evaluation 必须复用训练 split 的统计量，不能重新用目标环境全量数据估计。

归一化后 shape 不变：

```text
[B,3,114,64]
→
[B,3,114,64]
```

---

# 6. 阶段二：时间轴小波多尺度特征提取

## 6.1 小波变换方向

对每根天线、每个子载波对应的长度 64 时间序列单独进行小波分解：

```text
X_norm[b, antenna, subcarrier, :]
```

不在第一版中沿子载波轴做小波变换。

原因是本设计的核心假设是：

> 不同人体部位在 CSI 时序上表现为不同粒度的变化。

若同时沿子载波和时间做二维小波，将难以判断性能提升来自时间尺度还是子载波局部结构。

---

## 6.2 采用三级平稳小波变换

使用三级 Stationary Wavelet Transform。

推荐默认配置：

```yaml
wavelet:
  transform: swt
  family: db2
  levels: 3
  axis: time
```

第一版实现使用 `PyWavelets` 的 `pywt.swt`，并在依赖中加入：

```text
PyWavelets
```

实现时需要将 PyTorch tensor 转换到可执行 SWT 的表示，或在数据/模型工具层提供明确的适配器。若后续需要端到端 GPU 加速或可导小波变换，再单独实现基于固定滤波器的 PyTorch SWT。第一版优先保证正确性、可复现和消融验证。

三级分解产生：

```text
A3：第 3 级低频近似分量
D3：第 3 级细节分量
D2：第 2 级细节分量
D1：第 1 级细节分量
```

由于平稳小波变换不进行下采样，每个分量保持原时间长度：

```text
A3 ∈ R^[B,3,114,64]
D3 ∈ R^[B,3,114,64]
D2 ∈ R^[B,3,114,64]
D1 ∈ R^[B,3,114,64]
```

原始输入同时保留：

```text
R = X_norm ∈ R^[B,3,114,64]
```

因此，小波特征库为：

```text
W = stack(R, A3, D3, D2, D1)
```

shape 为：

```text
W ∈ R^[B,5,3,114,64]
```

维度语义为：

```text
[B, scale, antenna, subcarrier, time]
```

---

## 6.3 为什么保留原始输入分支

小波高频分量不一定全部对应有效的人体局部运动，也可能包含：

- CSI 测量噪声；
- 环境扰动；
- 傅里叶重采样带来的插值高频；
- 边界效应。

因此，模型不能只依赖小波系数。原始输入分支作为保底路径，使模型能够在小波特征无效时退回原始 amplitude 表征。

---

## 6.4 各尺度的设计假设

| 分量 | 主要频率性质 | 预期作用 |
|---|---|---|
| `R` | 全频段原始表示 | 保留全部输入信息 |
| `A3` | 最粗低频分量 | 骨盆、躯干、整体姿态 |
| `D3` | 较粗细节 | 大范围肢体方向 |
| `D2` | 中尺度细节 | 肘、膝等中间关节 |
| `D1` | 最细尺度细节 | 手腕、脚踝等远端关节，也可能包含较多噪声 |

这些是待验证假设，不在代码中写死关节与尺度的固定对应关系。

---

# 7. 阶段三：单尺度特征映射与尺度融合

## 7.1 单尺度特征映射器

每个尺度输入 shape 相同：

```text
X_s ∈ R^[B,3,114,64]
```

五个尺度使用同一个参数共享的特征映射器。

### 第一步：天线混合

使用：

```text
Conv2d(
    in_channels=3,
    out_channels=16,
    kernel_size=(1,1),
    stride=(1,1)
)
```

shape：

```text
[B,3,114,64]
→
[B,16,114,64]
```

设计理由：

1. 只在三根天线之间混合；
2. 不改变子载波和时间位置；
3. 学习不同接收链路对人体扰动的组合方式。

### 第二步：局部子载波—时间卷积

使用：

```text
Conv2d(
    in_channels=16,
    out_channels=32,
    kernel_size=(5,3),
    stride=(1,1),
    padding=(2,1)
)
```

shape：

```text
[B,16,114,64]
→
[B,32,114,64]
```

这里卷积核含义为：

```text
5 个相邻子载波 × 3 个相邻时间位置
```

卷积后使用：

```text
GroupNorm
GELU
```

得到：

```text
E_s ∈ R^[B,32,114,64]
```

五个尺度分别得到：

```text
E_R, E_A3, E_D3, E_D2, E_D1
```

---

## 7.2 为什么五个尺度共享映射器参数

共享参数有三个目的。

### 目的一：控制参数量

否则五套独立卷积会使模型规模显著增加，导致无法判断性能提升来自小波还是参数量。

### 目的二：统一特征空间

不同尺度被映射到同一个 32 维 latent feature space，后续才能合理加权融合。

### 目的三：增强实验可解释性

如果各尺度使用完全不同的编码器，则尺度差异与编码器差异混在一起，难以分析。

---

## 7.3 尺度身份嵌入

由于卷积参数共享，需要为每个尺度增加可学习尺度嵌入：

```text
e_s ∈ R^[1,32,1,1]
```

执行：

```text
E_s = E_s + e_s
```

shape 不变：

```text
[B,32,114,64]
```

这样网络仍能区分 raw、A3、D3、D2、D1。

---

## 7.4 粗尺度融合

粗尺度候选为：

```text
E_R, E_A3, E_D3
```

堆叠：

```text
E_coarse_stack ∈ R^[B,3,32,114,64]
```

对每个尺度做全局平均池化：

```text
mean over channel, subcarrier, time
```

得到：

```text
g_coarse ∈ R^[B,3]
```

经过两层 MLP：

```text
Linear(3,16)
GELU
Linear(16,3)
Softmax over scale
```

得到尺度权重：

```text
w_coarse ∈ R^[B,3]
```

最终融合：

```text
F_coarse_0 =
w_coarse[:,0] · E_R
+ w_coarse[:,1] · E_A3
+ w_coarse[:,2] · E_D3
```

输出：

```text
F_coarse_0 ∈ R^[B,32,114,64]
```

---

## 7.5 细尺度融合

细尺度候选为：

```text
E_R, E_D2, E_D1
```

同样得到：

```text
w_fine ∈ R^[B,3]
```

融合：

```text
F_fine_0 =
w_fine[:,0] · E_R
+ w_fine[:,1] · E_D2
+ w_fine[:,2] · E_D1
```

输出：

```text
F_fine_0 ∈ R^[B,32,114,64]
```

---

## 7.6 为什么粗细分支都包含 raw

粗分支包含 raw，可以防止低频分量丢失重要局部变化。

细分支包含 raw，可以防止 D1、D2 中的噪声主导特征。

因此 raw 是两个分支共享的残差基线。

---

# 8. 阶段四：粗尺度卷积编码器

粗尺度卷积编码器用于提取整体人体结构相关特征。

输入：

```text
F_coarse_0 ∈ R^[B,32,114,64]
```

---

## 8.1 粗尺度残差块一

主分支：

```text
Conv2d(32,64,kernel=3,stride=(2,2),padding=1)
GroupNorm
GELU
Conv2d(64,64,kernel=3,stride=1,padding=1)
GroupNorm
```

捷径分支：

```text
Conv2d(32,64,kernel=1,stride=(2,2))
```

输出相加后 GELU。

shape：

```text
[B,32,114,64]
→
[B,64,57,32]
```

尺寸计算：

```text
114 → 57
64  → 32
```

---

## 8.2 粗尺度残差块二

```text
channels: 64 → 128
stride: (2,2)
```

shape：

```text
[B,64,57,32]
→
[B,128,29,16]
```

---

## 8.3 粗尺度残差块三

```text
channels: 128 → 128
stride: (1,1)
```

shape：

```text
[B,128,29,16]
→
[B,128,29,16]
```

最终粗尺度卷积特征：

```text
F_coarse ∈ R^[B,128,29,16]
```

---

## 8.4 粗尺度编码器设计理由

1. 躯干和整体姿态变化范围较大，对时间精度要求相对低；
2. 两次对称下采样降低 token 数量；
3. 输出 29×16 个位置，适合建立全局骨架；
4. 作为后续完整基础姿态预测的主要信息来源。

---

# 9. 阶段五：细尺度卷积编码器

细尺度卷积编码器用于保留远端关节和局部变化信息。

输入：

```text
F_fine_0 ∈ R^[B,32,114,64]
```

---

## 9.1 细尺度残差块一

```text
channels: 32 → 64
stride: (2,1)
```

shape：

```text
[B,32,114,64]
→
[B,64,57,64]
```

只下采样子载波轴，不下采样时间轴。

设计理由：

> 第一层优先保留全部 64 个时间位置，避免细粒度时序模式过早丢失。

---

## 9.2 细尺度残差块二

```text
channels: 64 → 128
stride: (2,2)
```

shape：

```text
[B,64,57,64]
→
[B,128,29,32]
```

---

## 9.3 细尺度残差块三

```text
channels: 128 → 128
stride: (1,1)
```

shape：

```text
[B,128,29,32]
→
[B,128,29,32]
```

最终细尺度卷积特征：

```text
F_fine ∈ R^[B,128,29,32]
```

---

## 9.4 粗细分支差异

```text
粗尺度分支：[B,128,29,16]
细尺度分支：[B,128,29,32]
```

两者子载波分辨率相同，但细尺度分支时间分辨率为粗分支两倍。

这样可以在控制计算量的同时，重点为远端关节保留更多时间信息。

---

# 10. 阶段六：粗尺度轴向注意力编码器

轴向注意力分别沿子载波轴和时间轴建模，而不是直接对全部二维位置做全局注意力。

输入：

```text
F_coarse ∈ R^[B,128,29,16]
```

---

## 10.1 位置编码

加入两个独立可学习位置编码：

```text
P_frequency ∈ R^[1,128,29,1]
P_time      ∈ R^[1,128,1,16]
```

得到：

```text
F_coarse_pos =
F_coarse + P_frequency + P_time
```

shape 保持：

```text
[B,128,29,16]
```

---

## 10.2 子载波轴注意力

将每个时间位置视为一个独立序列：

```text
[B,128,29,16]
→ permute
[B,16,29,128]
→ reshape
[B×16,29,128]
```

在长度 29 的子载波序列上执行多头自注意力：

```text
embedding dimension = 128
num_heads = 8
```

输出 reshape 回：

```text
[B,128,29,16]
```

设计理由：

> 不同子载波对人体和环境的敏感程度不同，子载波注意力可以建立跨频率位置的全局依赖。

---

## 10.3 时间轴注意力

对子载波轴注意力输出执行：

```text
[B,128,29,16]
→ permute
[B,29,16,128]
→ reshape
[B×29,16,128]
```

在长度 16 的时间序列上执行多头自注意力。

输出恢复：

```text
[B,128,29,16]
```

设计理由：

> 在完成子载波信息聚合后，时间注意力建模整体时序演化。

---

## 10.4 通道投影

使用：

```text
Conv2d(128,256,kernel=1)
```

得到：

```text
E_coarse ∈ R^[B,256,29,16]
```

---

## 10.5 粗尺度 token 化

```text
[B,256,29,16]
→ permute
[B,29,16,256]
→ reshape
[B,464,256]
```

因为：

```text
29 × 16 = 464
```

最终：

```text
T_coarse ∈ R^[B,464,256]
```

---

# 11. 阶段七：细尺度轴向注意力编码器

输入：

```text
F_fine ∈ R^[B,128,29,32]
```

细尺度轴向编码器结构与粗尺度相同，但参数不共享。

---

## 11.1 位置编码

```text
P_frequency_fine ∈ R^[1,128,29,1]
P_time_fine      ∈ R^[1,128,1,32]
```

输出：

```text
[B,128,29,32]
```

---

## 11.2 子载波轴注意力

reshape：

```text
[B,128,29,32]
→
[B×32,29,128]
```

执行 8 头自注意力后恢复：

```text
[B,128,29,32]
```

---

## 11.3 时间轴注意力

reshape：

```text
[B,128,29,32]
→
[B×29,32,128]
```

执行 8 头自注意力后恢复：

```text
[B,128,29,32]
```

---

## 11.4 通道投影和 token 化

```text
Conv2d(128,256,kernel=1)
```

得到：

```text
E_fine ∈ R^[B,256,29,32]
```

展开：

```text
29 × 32 = 928
```

最终：

```text
T_fine ∈ R^[B,928,256]
```

---

## 11.5 为什么粗细轴向编码器不共享参数

粗分支和细分支面对不同统计特征：

- 粗分支强调稳定低频和整体结构；
- 细分支强调局部变化和高频信息。

共享全部注意力参数可能迫使两种分布使用同一编码规律。因此只保持结构相同，不共享权重。

---

# 12. 阶段八：Human3.6M-17 关节查询初始化

建立 17 个可学习关节查询：

```text
Q_parameter ∈ R^[17,256]
```

每个 query 固定对应一个 Human3.6M 关节。

扩展到 batch：

```text
Q ∈ R^[B,17,256]
```

这些 query 不直接表示坐标，而是表示：

> 每个关节应该从 CSI 特征中寻找什么样的信息。

---

# 13. 阶段九：粗尺度关节解码

## 13.1 输入

```text
query:  Q        ∈ [B,17,256]
memory: T_coarse ∈ [B,464,256]
```

## 13.2 单个粗尺度解码块

每个解码块包含：

### 第一步：关节到 CSI 的交叉注意力

```text
Q_attn = MultiHeadCrossAttention(
    query=Q,
    key=T_coarse,
    value=T_coarse
)
```

输出：

```text
[B,17,256]
```

### 第二步：残差与归一化

```text
Q = LayerNorm(Q + Q_attn)
```

### 第三步：前馈网络

```text
FFN:
Linear(256,1024)
GELU
Dropout
Linear(1024,256)
```

### 第四步：残差与归一化

```text
Q = LayerNorm(Q + FFN(Q))
```

建议堆叠两层。

最终得到：

```text
Z_coarse ∈ R^[B,17,256]
```

---

# 14. 阶段十：完整基础姿态预测

从粗尺度关节特征预测完整姿态：

```text
Z_coarse
[B,17,256]
    ↓ Linear(256,128)
[B,17,128]
    ↓ GELU
[B,17,128]
    ↓ Linear(128,2)
P_base
[B,17,2]
```

需要强调：

> 粗尺度分支预测的是完整 17 关节姿态，而不是只预测躯干。

原因是细尺度分支应作为修正路径，而不是唯一承担手腕和脚踝预测的路径。这样即使高频分量噪声较大，模型仍然有完整基础姿态输出。

---

# 15. 阶段十一：细尺度关节解码

## 15.1 细尺度 query 构造

利用粗尺度解码结果构造细尺度查询：

```text
Q_fine = LayerNorm(Q + Z_coarse)
```

shape：

```text
Q_fine ∈ R^[B,17,256]
```

设计理由：

> 细尺度搜索应基于已经建立的全局人体结构，而不是从零开始定位关节。

---

## 15.2 读取细尺度 token

输入：

```text
query:  Q_fine ∈ [B,17,256]
memory: T_fine ∈ [B,928,256]
```

使用与粗尺度解码块相同的两层交叉注意力结构，得到：

```text
Z_fine ∈ R^[B,17,256]
```

---

# 16. 阶段十二：关节级尺度门控

不同关节应独立决定是否使用细尺度信息。

## 16.1 门控输入

拼接：

```text
Z_coarse ∈ [B,17,256]
Z_fine   ∈ [B,17,256]
Q        ∈ [B,17,256]
```

得到：

```text
G_input ∈ R^[B,17,768]
```

---

## 16.2 门控网络

```text
Linear(768,128)
GELU
Linear(128,1)
Sigmoid
```

输出：

```text
alpha ∈ R^[B,17,1]
```

每个样本、每个关节都有独立门控值。

---

## 16.3 特征融合

```text
Z_fused =
Z_coarse + alpha ⊙ Z_fine
```

shape：

```text
[B,17,256]
```

---

## 16.4 门控初始化

最后一层 bias 建议初始化为：

```text
-2.0
```

初始：

```text
sigmoid(-2) ≈ 0.12
```

这意味着训练初始阶段模型主要依赖粗尺度路径，细尺度路径逐步学习加入。

这样可以降低高频噪声导致训练不稳定的风险。

---

# 17. 阶段十三：Human3.6M 骨架图传播

## 17.1 邻接矩阵

根据 17 个关节和 16 条骨骼边构建：

```text
A ∈ R^[17,17]
```

加入自环：

```text
A_self = A + I
```

归一化：

```text
A_norm = D^(-1/2) A_self D^(-1/2)
```

---

## 17.2 图传播

输入：

```text
Z_fused ∈ [B,17,256]
```

执行：

```text
Z_neighbor = A_norm @ Z_fused
```

得到：

```text
[B,17,256]
```

再经过线性变换：

```text
Linear(256,256)
GELU
```

使用残差：

```text
Z_graph =
LayerNorm(
    Z_fused + transformed(Z_neighbor)
)
```

shape：

```text
[B,17,256]
```

---

## 17.3 关节自注意力

在 17 个关节之间执行多头自注意力：

```text
query = key = value = Z_graph
```

输出：

```text
Z_refined ∈ R^[B,17,256]
```

图传播建模局部骨架邻接关系，自注意力补充非直接连接的全局人体关系。

---

# 18. 阶段十四：细尺度坐标残差预测

从骨架细化后的特征预测坐标残差：

```text
Z_refined
[B,17,256]
    ↓ Linear(256,128)
[B,17,128]
    ↓ GELU
[B,17,128]
    ↓ Linear(128,2)
Delta_P
[B,17,2]
```

最终输出：

```text
P_final =
P_base + alpha ⊙ Delta_P
```

其中：

```text
P_base  ∈ [B,17,2]
alpha   ∈ [B,17,1]
Delta_P ∈ [B,17,2]
```

广播后：

```text
P_final ∈ [B,17,2]
```

---

# 19. 完整 Shape 表

| 阶段 | 张量 | Shape |
|---|---|---|
| 模型输入 | `X` | `[B,3,114,64]` |
| 归一化输入 | `X_norm` | `[B,3,114,64]` |
| 原始分量 | `R` | `[B,3,114,64]` |
| 小波近似分量 | `A3` | `[B,3,114,64]` |
| 小波细节分量 | `D3` | `[B,3,114,64]` |
| 小波细节分量 | `D2` | `[B,3,114,64]` |
| 小波细节分量 | `D1` | `[B,3,114,64]` |
| 小波特征库 | `W` | `[B,5,3,114,64]` |
| 单尺度映射 | `E_s` | `[B,32,114,64]` |
| 粗尺度融合 | `F_coarse_0` | `[B,32,114,64]` |
| 细尺度融合 | `F_fine_0` | `[B,32,114,64]` |
| 粗残差块 1 |  | `[B,64,57,32]` |
| 粗残差块 2 |  | `[B,128,29,16]` |
| 粗残差块 3 | `F_coarse` | `[B,128,29,16]` |
| 细残差块 1 |  | `[B,64,57,64]` |
| 细残差块 2 |  | `[B,128,29,32]` |
| 细残差块 3 | `F_fine` | `[B,128,29,32]` |
| 粗轴向编码 | `E_coarse` | `[B,256,29,16]` |
| 细轴向编码 | `E_fine` | `[B,256,29,32]` |
| 粗 token | `T_coarse` | `[B,464,256]` |
| 细 token | `T_fine` | `[B,928,256]` |
| 关节查询 | `Q` | `[B,17,256]` |
| 粗关节特征 | `Z_coarse` | `[B,17,256]` |
| 基础姿态 | `P_base` | `[B,17,2]` |
| 细关节特征 | `Z_fine` | `[B,17,256]` |
| 关节尺度门控 | `alpha` | `[B,17,1]` |
| 融合关节特征 | `Z_fused` | `[B,17,256]` |
| 骨架细化特征 | `Z_refined` | `[B,17,256]` |
| 细尺度坐标残差 | `Delta_P` | `[B,17,2]` |
| 最终姿态 | `P_final` | `[B,17,2]` |

---

# 20. 损失函数设计

第一版建议使用三项损失：

```text
L_total =
L_final
+ λ_base L_base
+ λ_bone L_bone
```

---

## 20.1 最终坐标损失

```text
L_final =
mean(
    |P_final - P_gt|
)
```

输入：

```text
P_final, P_gt ∈ [B,17,2]
```

---

## 20.2 基础姿态辅助损失

```text
L_base =
mean(
    |P_base - P_gt|
)
```

作用：

1. 保证粗尺度路径能够独立预测完整姿态；
2. 防止模型把全部任务交给细尺度残差；
3. 让残差头真正学习修正量。

建议：

```text
λ_base = 0.3
```

---

## 20.3 骨骼长度损失

对每条骨骼边 `(i,j)`：

```text
l_pred(i,j) = ||P_pred_i - P_pred_j||_2
l_gt(i,j)   = ||P_gt_i - P_gt_j||_2
```

损失：

```text
L_bone =
mean_edges(
    |l_pred - l_gt|
)
```

建议：

```text
λ_bone = 0.1
```

---

## 20.4 可选的关节组平衡损失

关节分组：

```python
TORSO = [0, 7, 8, 9, 10]
PROXIMAL = [1, 4, 11, 14]
MIDDLE = [2, 5, 12, 15]
DISTAL = [3, 6, 13, 16]
```

分别计算组内平均损失：

```text
L_group =
(
    L_torso
    + L_proximal
    + L_middle
    + L_distal
) / 4
```

该项应作为独立消融，不建议第一轮与所有小波模块一起同时加入，否则难以判断性能提升来源。

---

# 21. 训练策略

## 21.1 阶段一：仅训练粗尺度路径

启用：

- raw、A3、D3；
- 粗尺度融合；
- 粗尺度卷积编码器；
- 粗尺度轴向注意力编码器；
- 粗尺度关节解码；
- 基础姿态头。

关闭：

- D2、D1；
- 细尺度卷积编码器；
- 细尺度轴向注意力编码器；
- 细尺度关节解码；
- 残差头。

目标：

> 先获得稳定的完整人体骨架预测。

---

## 21.2 阶段二：启用细尺度路径

加载阶段一权重。

启用：

- D2、D1；
- 细尺度融合；
- 细尺度卷积编码器；
- 细尺度轴向注意力编码器；
- 细尺度交叉注意力；
- 关节级尺度门控；
- 坐标残差头。

建议短时间冻结粗尺度路径，使细尺度分支先学习有效残差。

---

## 21.3 阶段三：全模型联合训练

解除所有冻结，联合优化：

```text
粗尺度路径
细尺度路径
关节门控
骨架传播
坐标输出
```

使用完整损失。

---

# 22. 跨环境少样本微调策略

模型应支持以下可训练参数组：

```text
scale_fusion_only
coarse_feature_encoder_only
fine_feature_encoder_only
coarse_attention_encoder_only
fine_attention_encoder_only
joint_decoder_only
joint_scale_gate_only
wavelet_related_modules_only
all_encoders
full_model
```

这样可以研究：

1. 跨环境变化主要影响粗尺度还是细尺度；
2. 是否只需调整尺度门控即可适配新环境；
3. 高频分支是否比低频分支更容易受环境噪声影响；
4. 少样本条件下 full fine-tuning 是否过拟合。

---

# 23. 基线模型的完整定义

为了公平验证小波设计，需要保留一个不使用小波的完整基线。

基线输入：

```text
[B,3,114,64]
```

基线结构：

```text
1×1 天线混合
[B,3,114,64]
→ [B,16,114,64]

5×3 局部子载波—时间卷积
[B,16,114,64]
→ [B,32,114,64]

残差块 1，stride=(2,2)
[B,32,114,64]
→ [B,64,57,32]

残差块 2，stride=(2,2)
[B,64,57,32]
→ [B,128,29,16]

残差块 3，stride=(1,1)
[B,128,29,16]
→ [B,128,29,16]

子载波轴注意力
[B,128,29,16]
→ [B,128,29,16]

时间轴注意力
[B,128,29,16]
→ [B,128,29,16]

1×1 通道投影
[B,128,29,16]
→ [B,256,29,16]

token 展开
[B,256,29,16]
→ [B,464,256]

17 个关节查询执行交叉注意力
[B,17,256]

Human3.6M 骨架图传播
[B,17,256]

关节自注意力
[B,17,256]

坐标回归
[B,17,256]
→ [B,17,2]
```

该基线完整保留：

- 三天线 CSI amplitude；
- 卷积编码；
- 子载波和时间轴注意力；
- 17 个关节查询；
- Human3.6M 骨架传播；
- 二维坐标回归。

它与新模型之间的核心差异只有：

```text
是否显式使用小波多尺度特征
是否使用粗细双分支
是否使用关节级尺度门控
是否使用基础姿态 + 细尺度残差
```

---

# 24. 消融实验设计

## 24.1 小波频带消融

| 实验 | Raw | A3 | D3 | D2 | D1 |
|---|---:|---:|---:|---:|---:|
| 纯原始输入 | ✓ |  |  |  |  |
| 原始 + 低频 | ✓ | ✓ | ✓ |  |  |
| 原始 + 高频 | ✓ |  |  | ✓ | ✓ |
| 全部小波尺度 | ✓ | ✓ | ✓ | ✓ | ✓ |
| 去除最细尺度 | ✓ | ✓ | ✓ | ✓ |  |

---

## 24.2 结构消融

```text
实验 A：
完整基线模型

实验 B：
基线模型 + 小波系数直接通道拼接

实验 C：
小波特征 + 粗细双卷积编码器

实验 D：
实验 C + 粗细双轴向注意力编码器

实验 E：
实验 D + 关节级尺度门控

实验 F：
实验 E + 基础姿态与细尺度残差解码

实验 G：
实验 F + Human3.6M 骨架图传播
```

---

## 24.3 时间分辨率消融

细尺度分支分别保留：

```text
16 个时间 token
32 个时间 token
64 个时间 token
```

用于验证远端关节性能是否确实受时间分辨率影响。

---

## 24.4 小波类型消融

```text
Haar / db1
Daubechies 2 / db2
Daubechies 4 / db4
```

第一版推荐 db2 作为默认，Haar 作为简单对照。

---

# 25. 评估指标

## 25.1 整体指标

```text
overall MPJPE
overall PCK
```

## 25.2 分粒度指标

```text
per-joint MPJPE
per-joint PCK
per-action MPJPE
per-environment MPJPE
```

## 25.3 关节组指标

```text
torso MPJPE
proximal-joint MPJPE
middle-joint MPJPE
distal-joint MPJPE
wrist MPJPE
ankle MPJPE
```

---

# 26. 可解释性诊断

训练和评估时必须记录：

1. 每个样本的粗尺度融合权重；
2. 每个样本的细尺度融合权重；
3. 每个关节的 `alpha`；
4. 各动作的平均 `alpha`；
5. 各环境的平均 `alpha`；
6. `P_base` 和 `P_final` 的误差差；
7. 每个关节的 `Delta_P` 平均幅值；
8. 去除 D1、D2 后各关节性能变化。

支持设计假设的理想证据为：

```text
手腕和脚踝的 alpha 高于 pelvis 和 thorax
        +
移除 D1/D2 后远端关节误差明显增加
        +
P_final 相比 P_base 主要改善手腕和脚踝
        +
躯干性能没有明显下降
```

---

# 27. 风险与限制

## 27.1 小波不能创造新信息

原始只有 10 个真实 packet。傅里叶重采样和小波变换只是重新表达已有信息。

## 27.2 高频分量可能主要是噪声

D1 和 D2 可能同时包含人体局部变化、无线噪声和插值伪影。

## 27.3 远端关节性能差不一定只由尺度导致

还可能来自：

- GT 噪声；
- 损失不平衡；
- 人体与无线传播之间的可辨识性不足；
- 跨环境分布偏移；
- 原始真实 packet 数量较少。

## 27.4 参数量增加造成不公平比较

实验必须同时报告：

```text
parameter count
FLOPs
training time
inference latency
GPU memory
```

必要时增加参数量匹配的纯卷积基线。

---

# 28. 推荐实现模块

```text
models/
├── input_normalization.py
├── temporal_stationary_wavelet.py
├── shared_scale_feature_mapper.py
├── sample_conditioned_scale_fusion.py
├── coarse_spatial_feature_encoder.py
├── fine_spatial_feature_encoder.py
├── coarse_axial_attention_encoder.py
├── fine_axial_attention_encoder.py
├── human36m_joint_query_initializer.py
├── coarse_joint_cross_attention_decoder.py
├── fine_joint_cross_attention_decoder.py
├── joint_specific_scale_gate.py
├── human36m_skeleton_graph_refiner.py
├── base_pose_regression_head.py
├── fine_pose_residual_head.py
└── wavelet_multiscale_csi_pose_model.py
```

---

# 29. 验收标准

实现完成后必须满足：

1. 输入严格为 `[B,3,114,64]`。
2. 输出严格为 `[B,17,2]`。
3. 所有关节定义统一为 Human3.6M-17。
4. 小波各分量 shape 均为 `[B,3,114,64]`。
5. 粗尺度卷积输出为 `[B,128,29,16]`。
6. 细尺度卷积输出为 `[B,128,29,32]`。
7. 粗 token 为 `[B,464,256]`。
8. 细 token 为 `[B,928,256]`。
9. 关节门控为 `[B,17,1]`。
10. 关闭细尺度路径时，基础姿态头仍可输出完整姿态。
11. 关闭全部小波模块时，可以运行完整非小波基线。
12. 项目中不再出现 OpenPose18 的关节数量、索引、邻接矩阵或变量语义。
13. 所有尺度、分支、损失和训练参数组均可通过配置独立启用或禁用。
14. 训练日志能够记录 overall、per-joint、joint-group 和尺度门控指标。

---

# 30. 最终设计结论

本模型通过以下完整路径解决原始单尺度卷积模型对远端关节不敏感的问题：

```text
三天线 CSI amplitude
        ↓
傅里叶重采样后的 64 点时序
        ↓
时间轴三级平稳小波分解
        ↓
粗尺度与细尺度显式分离
        ↓
不同时间分辨率的双卷积编码器
        ↓
分别执行子载波轴和时间轴注意力
        ↓
每个 Human3.6M 关节独立读取粗细尺度信息
        ↓
基础姿态预测
        +
细尺度关节残差
        ↓
Human3.6M 骨架结构细化
        ↓
17 个二维人体关键点
```

该设计不预设小波一定能够改善手腕和脚踝，而是通过完整的结构、消融和诊断机制验证以下假设：

> 粗尺度 CSI 特征主要支持人体整体结构和躯干定位；细尺度 CSI 特征可能为手腕、脚踝等远端关节提供额外局部证据；关节级尺度门控能够让不同关节按需使用细粒度信息，并抑制高频噪声对稳定姿态的破坏。
